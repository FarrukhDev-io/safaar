name: CI

# Faqat backend uchun — frontend (web-user/web-partner/web-admin) tekshiruvlari
# bu workflow'dan chiqarib tashlangan (backend deploy pipeline'ini
# frontend'dagi lint/build muammolari bloklamasligi uchun). Frontendlar
# Vercel orqali alohida deploy qilinadi va bu workflow'ga bog'liq emas.

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 1) Backend o'zgarganini aniqlaydi
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            shared: &shared
              - 'package.json'
              - 'package-lock.json'
              - 'packages/types/**'
            backend:
              - *shared
              - 'apps/backend/**'

  # 2) Backend o'zgargan bo'lsa — build + lint
  backend:
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build:types
      - run: npm run test -w @safaar/backend
      - run: npm run build:backend

  # 3) Boundary check — har bir commit faqat o'z papkasiga tegganligini tekshiradi
  boundary-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Papka chegaralarini tekshirish
        shell: bash
        run: |
          set -euo pipefail

          # Push yoki PR ekanligiga qarab base/head ni aniqlaymiz
          if [ "${{ github.event_name }}" = "push" ]; then
            BASE="${{ github.event.before }}"
            HEAD="${{ github.sha }}"
          else
            BASE="${{ github.event.pull_request.base.sha }}"
            HEAD="${{ github.event.pull_request.head.sha }}"
          fi

          # Agar BASE "0000..." bo'lsa (birinchi push), farqni topolmaymiz — skip
          if [ "$BASE" = "0000000000000000000000000000000000000000" ]; then
            echo "::notice::Birinchi push — boundary check o'tkazib yuborildi"
            exit 0
          fi

          VIOLATIONS=0

          for COMMIT in $(git rev-list "$BASE..$HEAD"); do
            FILES=$(git diff-tree --no-commit-id -r --name-only "$COMMIT")

            # Qaysi guruh(lar)ga tegishli ekanligini aniqlaymiz
            GROUP_OWNER=""

            while IFS= read -r FILE; do
              OWNER=""

              case "$FILE" in
                # @FarrukhDev-io
                apps/web-user/*)
                  OWNER="farrukh"
                  ;;
                # @adhambek7717
                apps/web-partner/* | apps/web-admin/*)
                  OWNER="adhambek"
                  ;;
                # @Lazizdeveloper
                apps/backend/* | packages/types/* | CODEOWNERS | tsconfig.json | .prettierrc)
                  OWNER="laziz"
                  ;;
                # Shared — hamma tegishi mumkin
                .github/* | *.md | AGENTS.md | CONTRIBUTING.md | .gitignore | .gitattributes)
                  OWNER="shared"
                  ;;
                railway.json | docker-compose* | vercel.json | .env* | next.config* | postcss.config* | eslint* | tsconfig* | package.json | package-lock.json)
                  OWNER="shared"
                  ;;
              esac

              # Agar OWNER aniqlanmasa — shared hisoblanadi
              OWNER="${OWNER:-shared}"

              if [ "$OWNER" != "shared" ]; then
                if [ -z "$GROUP_OWNER" ]; then
                  GROUP_OWNER="$OWNER"
                elif [ "$GROUP_OWNER" != "$OWNER" ]; then
                  echo "::error::CHEGARA BUZILISHI: $COMMIT — commit bir necha egaga tegishli"
                  echo "  Egalar: $GROUP_OWNER va $OWNER"
                  echo "  Fayl: $FILE"
                  VIOLATIONS=$((VIOLATIONS + 1))
                fi
              fi
            done <<< "$FILES"
          done

          if [ "$VIOLATIONS" -gt 0 ]; then
            echo "::error::$VIOLATIONS ta commitda chegara buzilishi aniqlandi"
            exit 1
          fi

          echo "::notice::Barcha commitlar chegara qoidalariga mos"

  # 4) Umumiy gate — branch protection uchun
  ci-success:
    name: CI Success
    needs: [backend, boundary-check]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Natijalarni tekshirish
        run: |
          results="${{ needs.backend.result }} ${{ needs.boundary-check.result }}"
          echo "Job natijalari: $results"
          for r in $results; do
            if [ "$r" = "failure" ] || [ "$r" = "cancelled" ]; then
              echo "::error::Kamida bitta tekshiruv muvaffaqiyatsiz tugadi."
              exit 1
            fi
          done
          echo "Barcha tegishli tekshiruvlar muvaffaqiyatli."

  # 5) Auto-merge: develop → main (faqat develop'ga push'da, CI to'liq o'tsa)
  auto-merge:
    needs: ci-success
    if: >
      github.ref == 'refs/heads/develop' &&
      github.event_name == 'push' &&
      success()
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
      - name: develop → main PR yaratish va auto-merge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --base main \
            --head develop \
            --title "🤖 Auto-merge: develop → main" \
            --body "## Avtomatik merge

          - **CI:** o'tdi ✅
          - **Boundary check:** o'tdi ✅

          Barcha tekshiruvlardan muvaffaqiyatli o'tgan develop'dagi o'zgarishlar main'ga merge qilinmoqda." \
            2>/dev/null || echo "PR allaqachon mavjud"

          gh pr merge --auto --squash 2>/dev/null || \
            echo "Auto-merge yoqilgan yoki PR yopiq"
